declare namespace Whoa {
    class WhoaUtil {
        public NumberPrecision(num: number, precision?: number): number;
    }
}

// eslint-disable-next-line no-var
declare var WhoaUtil: Whoa.WhoaUtil;
