{
    "name": "assets",
    "scripts": {
        "dev": "rollup --config",
        "build": "rollup --config"
    },
    "type": "module",
    "devDependencies": {
        "rollup-plugin-copy": "^3.5.0"
    }
}
